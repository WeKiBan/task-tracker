import { Box } from "@mui/material";

const Tasks = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "row",
        justifyContent: "space-evenly",
        alignItems: "center",
        width: "100%",
        height: "100vh",
      }}
      component="main"
    >
      TASKS
    </Box>
  );
};

export default Tasks;
